
def maximum_value(lista):
    max_lista = max(lista)
    return max_lista
    
def minimum_value(lista):
    min_lista = min(lista)
    return min_lista
def media(lista):
    media = sum(lista) / len(lista)
    return media

def sorting_list(lista):

    lista.sort()
    return lista



